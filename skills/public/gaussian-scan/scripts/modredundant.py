"""ModRedundant coordinate generation — the Gaussian-specific scan protocol.

Provides helpers to translate high-level scan requests into the `tail` block
appended to a `.gjf`. Full implementation in Step 6 of PLAN.md.
"""
