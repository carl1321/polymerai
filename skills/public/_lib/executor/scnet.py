"""SCNet API executor stub — port from `gaussian_agent.executors.scnet_api` later."""


class SCNetExecutor:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "SCNetExecutor will be ported from gaussian_agent in Step 5 of PLAN.md."
        )
